---
- name: Deploy Elasticsearch on primary node
  hosts: all
  become: true
  gather_facts: false
  vars:
    is_first_node: true
  pre_tasks:
    - name: Wait for SSH
      wait_for_connection:
        delay: 10
        timeout: 180
    - name: Gather facts
      setup:
  roles:
    - role: elasticsearch
